[project]
name = "agents-proxy"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
    "mypy>=1.13",
]

[tool.pytest.ini_options]
testpaths = ["proxy/tests"]

[tool.mypy]
strict = true

[tool.ruff]
line-length = 100
